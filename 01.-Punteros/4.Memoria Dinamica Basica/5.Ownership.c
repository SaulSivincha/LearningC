#include <stdio.h>
#include <stdlib.h>

float *crearNotas(int cantidad) {
    // reservar memoria con malloc

    // verificar si malloc fallo

    // devolver el puntero
}

void llenarNotas(float *notas, int cantidad) {
    // pedir cada nota y guardarla en el arreglo
}

void imprimirNotas(float *notas, int cantidad) {
    // imprimir todas las notas
}

float calcularPromedio(float *notas, int cantidad) {
    // sumar todas las notas

    // devolver promedio
}

int main(void) {
    int cantidad = 0;

    printf("Cantidad de estudiantes: ");
    scanf("%d", &cantidad);

    // validar cantidad

    // crear arreglo llamando a crearNotas

    // verificar si devolvio NULL

    // llenar notas

    // imprimir notas

    // calcular promedio

    // mostrar promedio

    // liberar memoria

    return 0;
}